# .env.example
# -----------------------------------------------------------------------------
# Takeovers Bilan Extraction Pipeline Configuration
# -----------------------------------------------------------------------------
# This pipeline runs 100% locally using Takeovers' provided OCR JSON data,
# deterministic string caches, OpenCV geometric routines, and Scikit-Learn 
# unsupervised column clustering.
#
# No external LLM, paid vision API, or cloud provider credentials are required.
# Marginal cost per page: 0.00 EUR.
# -----------------------------------------------------------------------------

